name = pbr_census
description = Throwaway worldmod: counts the node faces a player can see across a sample of generated terrain, then shuts the server down. Never install it on a world anyone plays.
optional_depends = mcl_init, mcl_villages
