name = terrain_diffusion
title = Terrain Diffusion
description = Map generator fed by a diffusion model trained on real elevation and climate data. Ground comes from whatever biomes the game registers. Needs a tile cache baked into the world first.
optional_depends = mcl_core, mcl_trees, mcl_flowers, default, goanna_server_mod
min_minetest_version = 5.9
